// type LogoProps = {
//   fontSize?: string;
//   fontWeight?: string;
// };

const Logo = () => {
  // const { fontSize = "5xl" } = props;
  // const { fontWeight = "bold" } = props;

  return <h1 className={`text-blue-500 text-5xl font-bold`}>CHATTER</h1>;
};

export default Logo;
