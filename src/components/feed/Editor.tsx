import React from "react";
import PrimaryButton from "../base/PrimaryButton";

const Editor = () => {
  return (
    <section className="m-[30px] border border-b-black-100 h-screen">
      <PrimaryButton value="Publish" handleClick={() => {}} />
    </section>
  );
};

export default Editor;
